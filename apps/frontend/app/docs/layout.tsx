import React from 'react';

export const metadata = {
    title: 'Documentation | ProjectMaster',
    description: 'ProjectMaster API and developer documentation',
};

export default function DocsRootLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return children;
}
